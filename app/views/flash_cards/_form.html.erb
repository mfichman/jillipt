<%= form_for(@flash_card) do |f| %>
  <% if @flash_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flash_card.errors.count, "error") %> prohibited this flash_card from being saved:</h2>

      <ul>
      <% @flash_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :front %><br />
    <%= f.text_area :front %>
  </div>
  <div class="field">
    <%= f.label :back %><br />
    <%= f.text_area :back %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
